بلوپرینت نهایی پروژه کتاب‌یار (نسخه ۱.۰)

سند مرجع کامل برای ساخت پلتفرم کتاب‌خوان پریمیوم، دوزبانه و هوشمند کتاب‌یار

۱. 🎯 چشم‌انداز پروژه (Project Vision)

ساخت یک پلتفرم مدرن و پریمیوم آنلاین خواندن کتاب با پشتیبانی دوزبانه (انگلیسی/فارسی) که از نظر کیفیت طراحی و تجربه کاربری با سرویس‌های استریم مانند نتفلیکس رقابت کند. پلتفرم باید حسی از پیچیدگی، روانی و جذابیت داشته باشد و پالت رنگ اصلی آن بر پایه تناژهای طلایی/بژ (رنگ‌های گرم و شیک، نه زرد روشن) استوار باشد.

اهداف کلیدی:

تجربه خواندن غوطه‌ورکننده: شبیه‌سازی حس خواندن کتاب فیزیکی با UI مدرن.

یادگیری هوشمند: ادغام ابزارهای یادگیری زبان مبتنی بر AI.

گیمیفیکیشن جذاب: تبدیل خواندن به یک بازی اعتیادآور.

کشف هوشمند: سیستم پیشنهاد کتاب شخصی‌سازی شده.

مدل فریمیوم پایدار: جذب کاربر رایگان و تبدیل به مشترک پولی.

عملکرد بی‌نقص: سریع، بهینه و رایگان برای شروع.

۲. 🎨 فلسفه طراحی (Design Philosophy)

هویت بصری

رنگ اصلی: طلایی/بژ شیک (#D4AF37, #C9A961, #B8956A).

الهام‌بخش: نتفلیکس، دیزنی+، Apple Books، Medium (حس پریمیوم و محتوا محور).

سبک: مدرن، تمیز، مینیمال با انیمیشن‌های هدفمند.

حالت تاریک (Dark Mode): پشتیبانی کامل با ترنزیشن‌های نرم.

واکنش‌گرا (Responsive): بی‌نقص در دسکتاپ، تبلت و موبایل.

تایپوگرافی: Inter (انگلیسی)، Vazirmatn (فارسی) با پشتیبانی کامل RTL.

استانداردهای انیمیشن (با همکاری ایجنت ۲ و ۳)

رویکرد: انیمیشن‌ها باید «حس» درستی بدهند (ایجنت ۳) اما «بهینه» باشند (ایجنت ۲).

تکنیک: استفاده انحصاری از transform و opacity (شتاب‌دهی GPU) با Framer Motion.

ترنزیشن‌های صفحه: ۳۰۰-۵۰۰ میلی‌ثانیه cubic-bezier.

میکرو-تعاملات (Hover/Click): ۱۵۰-۲۵۰ میلی‌ثانیه ease-out.

ورق زدن (MVP): انیمیشن Slide یا Fade سریع و بهینه (۲۵۰ میلی‌ثانیه).

ورق زدن (فاز ۲ - اختیاری): انیمیشن مبتنی بر فیزیک (۶۰۰-۸۰۰ میلی‌ثانیه) با گزینه فعال/غیرفعال‌سازی در تنظیمات.

انیمیشن‌های پاداش (XP، اتمام فصل): کوتاه، جذاب و فوری (۳۰۰ میلی‌ثانیه تا ۲ ثانیه).

حالات لودینگ: Skeleton Screens (با shadcn/ui)، نه Spinner.

۳. معماری کلان و استراتژی رندر (تصمیمات ایجنت ۱ و ۲)

ما از معماری هیبریدی (Hybrid) با Next.js استفاده می‌کنیم تا هم SEO عالی داشته باشیم و هم هزینه سرور رایگان بماند.

الف) منطقه عمومی (Public Zone) - برای گوگل و کاربران جدید

صفحات: /, /about, /help, /books/[slug] (صفحه معرفی کتاب).

استراتژی رندر: SSG (Static Site Generation).

هدف: حداکثر سرعت، بهترین SEO، فشار صفر روی سرور Vercel. Vercel فایل‌های HTML از پیش ساخته شده را سرو می‌کند.

ب) منطقه خصوصی (Private Zone) - برای کاربران لاگین کرده

صفحات: /dashboard, /library, /profile, /vocabulary, /books/read/[slug] (کتاب‌خوان).

استراتژی رندر: CSR (Client-Side Rendering).

هدف: فشار صفر روی سرور Vercel، اجرای تمام منطق روی دستگاه کاربر، تجربه اپلیکیشن-مانند. Vercel فقط یک پوسته خالی اولیه ارسال می‌کند.

فعال‌سازی PWA: این بخش به یک Progressive Web App (PWA) تبدیل می‌شود تا قابلیت نصب و اجرای آفلاین داشته باشد.

۴. 🛠 پشته فنی (Technical Stack)

فرانت‌اند

فریم‌ورک: Next.js 15+ (App Router)

زبان: TypeScript 5+

استایل: Tailwind CSS 3+

کامپوننت UI: shadcn/ui (Radix UI)

انیمیشن: Framer Motion

مدیریت وضعیت: Zustand (برای وضعیت کلاینت) + React Context (برای تم و کاربر)

فرم: React Hook Form + Zod

آیکون: Lucide React

فونت: next/font (Inter, Vazirmatn)

بک‌اند و زیرساخت

دیتابیس: Supabase (PostgreSQL)

احراز هویت: Supabase Auth

ذخیره‌سازی فایل: Supabase Storage (کاورها، محتوای کتاب‌ها به صورت JSON)

هاستینگ: Vercel (برای Next.js Frontend)

API: Next.js API Routes (برای کارهای خاص مانند ارتباط با AI)

ابزارهای کلیدی دیگر

ادیتور متن (ادمین): TipTap (با تنظیمات پایه برای MVP)

چارت: Recharts

تاریخ: date-fns + date-fns-jalali

دیتا فچینگ (کلاینت): TanStack Query (React Query) v5+

AI: Google Gemini 2.5 Flash (@google/generative-ai)

پرداخت: Stripe (برای فازهای بعدی)

PWA: next-pwa

۵. 🚀 محصول اولیه حداقلی (Minimum Viable Product - MVP)

هدف MVP، لانچ سریع برای تست ارزش اصلی (خواندن دوزبانه + گیمیفیکیشن پایه) و شروع کسب درآمد است. تمام ویژگی‌های غیرضروری به فازهای بعدی موکول می‌شوند.

ویژگی‌های داخل MVP:

زیرساخت:

راه‌اندازی پروژه Next.js روی Vercel.

راه‌اندازی Supabase (Auth, DB, Storage).

احراز هویت:

ثبت‌نام و ورود با ایمیل (Supabase Auth).

صفحات عمومی (SSG):

صفحه اصلی (app/page.tsx) با طراحی اولیه و نمایش چند کتاب.

صفحات معرفی کتاب (app/books/[slug]/page.tsx) برای ۱۰ تا ۲۰ کتاب پرفروش اولیه (با استفاده از generateStaticParams). شامل خلاصه، کاور و دکمه "شروع خواندن".

کتاب‌خوان اصلی (CSR - app/books/read/[slug]):

خواندن محتوای کتاب از Supabase Storage (فرمت JSON).

نمایش متن کامل (تقسیم‌بندی به صفحات در سمت کلاینت).

"دکمه جادویی" سوییچ بین فارسی و انگلیسی.

دیکشنری پاپ‌آپ (با کلیک روی کلمه انگلیسی).

انیمیشن ورق زدن ساده (Slide یا Fade).

گیمیفیکیشن پایه:

سیستم XP (محاسبه در use-book-tracking.ts بر اساس اسکرول/زمان).

سیستم Streak (استریک) روزانه.

نمایش XP و Streak در یک داشبورد بسیار ساده (app/dashboard/page.tsx).

مدیریت لغات (پایه):

امکان ذخیره کلمات از دیکشنری (با محدودیت ۲۰ کلمه برای کاربران رایگان) در جدول vocabulary.

نمایش لیست کلمات ذخیره شده در app/vocabulary/page.tsx.

مدل فریمیوم (پایه):

ارائه ۲-۳ کتاب پرفروش + چند کتاب کلاسیک به صورت کاملاً رایگان.

نمایش دکمه/بنر "ارتقا به پرمیوم" برای دسترسی به بقیه کتاب‌ها و لغات نامحدود.

(سیستم پرداخت واقعی می‌تواند در فاز ۱.۵ اضافه شود؛ MVP اولیه می‌تواند فقط دکمه ارتقا را داشته باشد).

پنل ادمین (فوق‌العاده ساده):

یک صفحه محافظت شده (app/admin-secure...) فقط برای آپلود فایل‌های JSON کتاب‌ها به Supabase Storage و وارد کردن اطلاعات اولیه کتاب (عنوان، نویسنده، کاور) در جدول books. (بدون ادیتور TipTap پیشرفته در MVP).

ویژگی‌های خارج از MVP (برای فازهای بعدی):

انیمیشن ورق زدن واقعی‌گرایانه.

ادیتور TipTap پیشرفته در پنل ادمین.

سیستم کامل لیگ‌ها، دوستان، نشان‌ها (Badges).

سیستم فلش‌کارت پیشرفته (Spaced Repetition).

چت با AI.

سیستم پیشنهاد کتاب.

سیستم کامنت‌گذاری و امتیازدهی.

صفحه پروفایل کاربر پیشرفته.

جستجو و فیلتر پیشرفته در کتابخانه.

دانلود آفلاین (PWA آفلاین در فاز بعدی پیاده‌سازی شود).

سیستم پرداخت کامل Stripe.

وبلاگ.

صفحات About, Help, Support.

دارک مود (می‌تواند به سادگی با next-themes اضافه شود اما جزو اولویت اصلی MVP نیست).

۶. 📊 ساختار دیتابیس (Supabase PostgreSQL - بهینه‌سازی شده)

تغییر کلیدی: جدول book_content حذف شده است. محتوای کتاب‌ها در Supabase Storage نگهداری می‌شود.

جداول اصلی (برای MVP):

users

id (uuid, primary key, default: auth.uid())

email (text, unique)

name (text, nullable)

avatar_url (text, nullable)

created_at (timestamp with time zone, default: now())

updated_at (timestamp with time zone, default: now())

preferences (jsonb, nullable) - برای ذخیره زبان پیش‌فرض UI

xp (integer, default: 0)

current_streak (integer, default: 0)

last_read_at (date, nullable)

subscription_tier (text, default: 'free') - برای Paywall

subscription_expires_at (timestamp with time zone, nullable)

books

id (uuid, primary key, default: gen_random_uuid())

title (text, not null)

slug (text, unique, not null) - برای آدرس URL

author_id (uuid, foreign key references authors)

publication_year (integer, nullable)

summary (text, nullable)

cover_image_url (text, nullable)

content_json_url_en (text, nullable) - آدرس فایل انگلیسی در Storage

content_json_url_fa (text, nullable) - آدرس فایل فارسی در Storage

free_preview_pages (integer, default: 20)

total_pages (integer, nullable) - برای محاسبه پیشرفت

genres (text[], nullable) - آرایه‌ای از نام ژانرها (ساده شده برای MVP)

status (text, default: 'draft') - draft, published, archived

created_at (timestamp with time zone, default: now())

updated_at (timestamp with time zone, default: now())

authors

id (uuid, primary key, default: gen_random_uuid())

name (text, not null)

bio (text, nullable)

photo_url (text, nullable)

created_at (timestamp with time zone, default: now())

user_library (برای پیگیری وضعیت خواندن کاربر)

user_id (uuid, foreign key references users, primary key)

book_id (uuid, foreign key references books, primary key)

status (text, default: 'want_to_read') - want_to_read, reading, completed

current_page (integer, default: 0)

progress_percentage (integer, default: 0)

started_at (timestamp with time zone, nullable)

completed_at (timestamp with time zone, nullable)

updated_at (timestamp with time zone, default: now())

vocabulary (برای لیست کلمات کاربر)

id (uuid, primary key, default: gen_random_uuid())

user_id (uuid, foreign key references users)

word (text, not null)

definition (text, nullable)

book_id (uuid, foreign key references books, nullable)

context (text, nullable) - جمله‌ای که کلمه در آن بوده

created_at (timestamp with time zone, default: now())

mastery_level (integer, default: 0) - برای فلش‌کارت آینده

(جداول دیگر مانند reviews, highlights, achievements برای فازهای بعدی هستند)

۷. 🚀 فازهای توسعه (بازبینی شده با تمرکز بر MVP)

فاز ۱: MVP (ماه‌های ۱ تا ۴)

هدف: لانچ نسخه اولیه با هسته اصلی خواندن دوزبانه و گیمیفیکیشن پایه.

اقدامات:

راه‌اندازی زیرساخت (Next.js, Vercel, Supabase).

پیاده‌سازی Auth.

ساخت صفحات SSG (اصلی، معرفی ۱۰-۲۰ کتاب).

ساخت کتاب‌خوان CSR با سوییچ زبان و دیکشنری.

پیاده‌سازی سیستم XP و Streak (پایه).

ساخت صفحه ساده داشبورد و لغات.

ساخت پنل ادمین فوق‌العاده ساده (فقط آپلود JSON و اطلاعات پایه).

پیاده‌سازی مدل فریمیوم (قفل محتوا).

فاز ۲: ارتقا و جذب کاربر (ماه‌های ۵ تا ۸)

هدف: بهبود تجربه، شروع درآمدزایی، جذب ۱۰۰۰ کاربر اول.

اقدامات:

پیاده‌سازی سیستم پرداخت Stripe.

تکمیل صفحه کتابخانه (/library) با فیلترهای پایه.

بهبود UI کتاب‌خوان (افزودن تنظیمات فونت، تم).

اضافه کردن سیستم لیست دوستان (Friends).

پیاده‌سازی کامل لیگ‌های هفتگی.

ساخت صفحه پروفایل کاربر.

شروع بازاریابی محتوایی و تبلیغات اولیه.

تبدیل به PWA و افزودن قابلیت دانلود آفلاین امن.

فاز ۳ به بعد:

اضافه کردن فیچرهای پیشرفته (انیمیشن ورق زدن، AI Chat، فلش‌کارت پیشرفته، پیشنهاد کتاب، سیستم کامنت، وبلاگ، و...).

۸. استراتژی‌های کلیدی (خلاصه تحلیل ایجنت‌ها)

الف) استراتژی SEO (ایجنت ۱)

تمرکز کامل بر صفحات SSG (مخصوصاً app/books/[slug]) با متادیتای داینامیک و JSON-LD.

مسدود کردن بخش خصوصی (CSR) از طریق robots.txt.

ساخت خودکار sitemap.xml.

توجه ویژه به Core Web Vitals (LCP, CLS, INP) از طریق SSG و بهینه‌سازی تصاویر/فونت‌ها.

ب) استراتژی بهینه‌سازی (ایجنت ۲)

استفاده حداکثری از CSR برای بخش اپلیکیشن (فشار صفر روی Vercel).

معماری PWA برای عملکرد آفلاین و حس اپلیکیشن.

ذخیره‌سازی محتوای کتاب‌ها در Supabase Storage (نه دیتابیس) برای حداکثر کارایی.

بهینه‌سازی انیمیشن‌ها (GPU-based).

Code Splitting برای کامپوننت‌های سنگین (reader, AI, admin editor).

بهینه‌سازی فونت و تصویر با ابزارهای Next.js.

ج) استراتژی اعتیادآوری (ایجنت ۳)

مدل فریمیوم «دولینگو + اسپاتیفای» (قلاب گیمیفیکیشن رایگان، محتوای پرمیوم پولی).

تمرکز بر استریک روزانه (Loss Aversion).

بازخوردهای بصری/صوتی فوری برای XP.

استفاده هوشمندانه از روانشناسی انتظار (لودینگ، کوئیز).

شخصی‌سازی و غافلگیری‌های کوچک.

طراحی هدفمند انیمیشن‌ها برای ایجاد «حس» درست.

این بلوپرینت، نقشه راه کامل و به‌روز شده شما برای ساخت «کتاب‌یار» است.